# Floworio API Dependencies

# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# Data processing (from sjvisualizer)
pandas>=2.0.0
numpy>=1.26.0
openpyxl>=3.1.0

# AI Story Generation (optional — set API keys in .env)
openai>=1.12.0
anthropic>=0.18.0

# TTS — Kokoro (open-source)
# Install separately: pip install kokoro soundfile
# kokoro>=0.9.0
# soundfile>=0.12.0

# HTTP client for social media APIs
httpx>=0.27.0

# Environment variables
python-dotenv>=1.0.0

# CORS
starlette>=0.36.0

# Async job queue (production)
# celery>=5.3.0
# redis>=5.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
